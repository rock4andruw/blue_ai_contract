# 待辦清單（下一步）

> 這是**滾動更新的行動清單**，只放「還沒做完的事」，不放歷史敘事。完成的項目直接刪除，不留勾選記錄——歷史脈絡已經在 `archive/next_step_plan.md`（舊版全文，含詳細開發歷程）跟 git log 裡，不需要在這裡重複。
> 每次做完一件事，回來把這份文件更新掉，不要留著讓它變成第二個過時文件。
> 最後更新：2026-07-03

**競賽截止**：2026 年 8 月初｜**評審**：AI / AP / Infra 部長 + 黑客松評審

---

## ✅ 剛完成（2026-07-04）：對話式法務助理 Proof of Concept

`docs/planning/agent_db_mcp_roadmap.md` 的「對話式法務助理」願景，其中不需要新資料源的兩塊已經做出來並端到端測試通過：

- **報告內問答**（`POST /api/v1/contracts/ask`）：`llm_service.py` 新增 `answer_report_question()`，錨定在當次報告的 `key_changes`，查無資訊誠實回答「這份報告沒有相關資訊」。實測 3 題：問風險理由（正確引用 trigger_reason）、問法律依據（正確引用 `legal_basis`，含民法216/252條）、問報告沒涵蓋的問題（正確拒答，沒有編造）。
- **協商矩陣**（`POST /api/v1/contracts/negotiate/matrix`）：`negotiate_service.py` 新增 `generate_matrix()`，批次呼叫既有 `generate_playbook()`，組成「條款/風險/對方訴求/我方政策/建議折衷方案/底線/法律依據」對照表，含 Markdown 表格輸出。實測 v4 全部 10 個高風險條款，內容完整、格式正確。

兩者都是純組裝層，複用既有的風險摘要、三層對策、Layer 3 法律依據，沒有新增判斷邏輯、沒有新資料源。

**✅ 前端 UI 也做完了（2026-07-05）**：`demo.html` 延續既有的法律文件 editorial 視覺語言（襯線標題、mono 大寫標籤、左邊框色塊表語意），不是另外做一套聊天 UI。問答用「Q/A 批註」形式，答案依「有依據／查無資料」呈現兩種明顯不同的視覺狀態（金色實線 vs 灰色虛線），把誠實揭露原則做成看得見的介面語言；協商矩陣用可列印的附表形式（`@media print` 支援直接列印/存 PDF）。用 Playwright 實測：問答的 grounded/ungrounded 兩態、矩陣表格生成、手機版排版皆正常，抓到並修復一個 markdown 粗體符號沒轉譯的顯示 bug。

**尚未做**：2.1-2.3（歷史合約庫/MCP擴充/Insight分析）維持路線圖階段未動手，這是刻意的（需要真實資料或基礎設施）。

## 🔴 高優先

1. **簡報製作**：目前完全還沒開始。技術內容已經足夠扎實（見 `docs/architecture/合約比對解法.md`），現在最大風險是「東西做得好但沒包裝出來」。
2. **Demo 流程預演 + 部長話術排練**：把已經準備好的追問答案（資料庫設計、Layer2 讀不讀全文、為什麼不全部向量化、為什麼不找外部工具取代規則引擎）真的練習說出口，不是寫在文件裡就好。
3. **文字守恆檢查（coverage guard）實作**：`docs/architecture/合約比對解法.md` 第五節缺口 #1。純字元計數比對，不需要 LLM，半天內可測完。直接強化「誠實揭露」敘事——不只是嘴上講誠實，系統有一層機制數學上保證沒有內容被靜默漏看。

## 🟡 中優先

5. **NDA 保密期規則驗證**：`RISK_CONFIDENTIALITY_WEAKENED`（5年→2年）手上的真實 NDA 沒有這個變更情境，規則邏輯還沒被真實資料驗證過。
6. **舊架構圖檔清理**：`image/PROJECT_PLAN/系統架構圖_比賽Demo版_20260703.*`（Phase2/3 完整節點版）已被現況圖+路線圖的拆分取代，確認不再需要後可以刪除；`1782466910201.png`（最舊版，PROJECT_PLAN.md 原本嵌的圖）也一併確認是否還有用途。
7. **架構圖 PNG 未進版控**：`.gitignore` 的 `系統架構圖*.png` 規則擋掉了 `系統架構圖_現況_20260703.png`（SVG 版沒被擋，已進版）。`PROJECT_PLAN.md` 目前用 `.png` 路徑嵌入，clone 這個 repo 的人會看到圖片失效。2026-07-04 已跟你確認過，暫緩處理——之後要嘛把嵌入路徑改成 `.svg`，要嘛調整 gitignore 規則強制加入 PNG。

## 🟢 低優先／可選

7. **`candidate_rules.jsonl` 複核**：目前累積 403 筆 Verification Agent 的 Case C 發現（截至 2026-07-03，筆數會持續增加）。跨合約重複分析顯示：
   - 「違約金/罰則」「保護條款」都跨了 6/9 組合約，已達「重複出現才升級」門檻，可考慮手寫成正式規則
   - 「新類別候選（未歸類）」跨 5/9 組，內容集中在「刪除續約/銜接條款」「刪除加價服務條款」——現有 15 條規則跟分類法完全沒覆蓋，是真實存在的空白
   - 詳細分析方法見 memory（`project_contract_diff.md`）或重新執行去重統計
8. ✅ **NLI 框架實驗（2026-07-04，已測試，不採用）**：把 Verification Agent 的判斷框架從「開放式風險評估」改成「蘊含/矛盾/中立」三分類。已完整 A/B 測試（同一組 v2-v6 範例，同一份備份還原機制），結果是**明顯退步，不是改善**：(1) v6 旗艦案例（千分之一費率）從 2 筆獨立發現被合併成 1 筆，clause_id 也從有意義的子標籤退化成純數字；(2) v2 完全漏掉一筆原本有抓到的 high 風險發現（P1/P2/P3 修復時效惡化）；(3) v5 同一條款的 risk_level 從 high 被降成 medium，且漏掉另一筆原本抓到的發現。已依測試前訂好的判準（沒有明顯改善就不採用）還原 `verifier.py` 至原版並驗證 v6 正確恢復。**結論：不採用，不用再測第二次**——除非之後有不同的 prompt 設計方式，否則這個方向已經有明確的反面證據。
9. **語意殘餘配對**（對齊層第 3 層）：**先做根因分析再決定**——用現有 38 筆 gold set 查 Overall Detection 67% 的缺口是「配對錯」還是「配對對但規則沒判出」。若是後者，做語意對齊完全不會改善這個數字，不要先選武器再找戰場。

---

## 不做（賽前）

真實 PostgreSQL 資料庫、異質模型 MAS、Teams/SharePoint 整合、大規模資料持久化、雙欄排版 PDF 解析（Docling，除非遇到真實案例）。理由與技術備忘見 `archive/next_step_plan.md`。
