![1783402292876](image/next_step_plan/1783402292876.png)# 待辦清單（下一步）

> 這是**滾動更新的行動清單**，只放「還沒做完的事」，不放歷史敘事。完成的項目直接刪除，不留勾選記錄——歷史脈絡已經在 `archive/next_step_plan.md`（舊版全文，含詳細開發歷程）跟 git log 裡，不需要在這裡重複。
> 每次做完一件事，回來把這份文件更新掉，不要留著讓它變成第二個過時文件。
> 最後更新：2026-07-11

**發表時程（2026-07-11 正式來信更新，取代先前「8 月初截止」的舊認知）**：發表安排在 7、8、9 月大 P 月會，每月最多 2 組，依回覆優先順序排定，額滿為止——**沒有單一硬性截止日，是搶名額制**。需要組長回信：(1) 確認題目範圍免調整或提出調整建議、(2) 選定希望發表的月份。｜**評審**：發表現場參加的大 P，計分項目：技術性/實用性/商機推廣各 1-10 分，採在場大 P 平均分

---

## ✅ 已完成（截至 2026-07-09，不再重複列出）

- 對話式法務助理 PoC（報告內問答 + 協商矩陣，含前端 UI）
- 文字守恆檢查（Coverage Guard）：已實作並接入 `contracts.py`/`orchestrator.py`，v1/v4 實測 99.3%/98.9%
- 黑客松簡報大綱 + 講稿（`champion_pitch_hackathon.md`/`presenter_script_hackathon.md`）：主線、三部長定位、數字全面校正（高風險 5/5、整體偵測率 54%/12-22、非 67%/12-18）
- System Prompt 清冊建立（`docs/architecture/技術棧.md`），含 6 個 prompt 完整盤點 + 死資產辨識
- Ask 功能 grounded 判斷修復（字串比對 → JSON 結構化輸出，fail-then-pass 驗證）
- `report-writing.md` 已實測比較（LLM 組裝報告 vs 純 Python），確認現況架構正確，不採用

## 🔴 高優先

1. **Demo 流程預演 + 部長話術排練**：把已經準備好的追問答案（資料庫設計、Layer2 讀不讀全文、為什麼不全部向量化、殘留風險怎麼辦）真的練習說出口，不是寫在文件裡就好。**目前完全沒有實際排練過**，只有文字稿。
2. **確認簡報時間**：`champion_pitch_hackathon.md`/`presenter_script_hackathon.md` 內部段落時間加總對不上宣稱的「20 分鐘」（實測只有 ~15-15.5 分鐘），需要真人唸一次抓真實秒數。

## 🟡 中優先

~~1. NDA 保密期規則驗證~~ ✅ **2026-07-09 已完成並修復真實 bug**：`_extract_years()` 原本只認阿拉伯數字，中文數字「五年→二年」（台灣合約常見寫法）完全漏判，已用合成測試案例重現並修復（fail-then-pass 驗證，回歸測試無退化）。詳見 `docs/architecture/技術棧.md` Step 4。
2. **舊架構圖檔清理**：`image/PROJECT_PLAN/系統架構圖_比賽Demo版_20260703.*` 確認不再需要後可刪除；`1782466910201.png` 一併確認用途。
3. **架構圖 PNG 未進版控**：`.gitignore` 規則擋掉 `系統架構圖_現況_20260703.png`，`PROJECT_PLAN.md` 用 `.png` 路徑嵌入會失效。已跟你確認過暫緩處理。

## 🟢 低優先／可選

1. **`candidate_rules.jsonl` 複核（2026-07-16 重新分析，修正先前過於樂觀的解讀）**：累積到 575 筆，但**大部分是同一批合約被反覆測試累積出來的重複記錄，不是獨立案例**（LLM 每次重跑同一份合約會用不同措辭重新描述同一個發現）。實際細查後：
   - **真正跨多組不同合約重複、達到升級門檻的只有「智財侵權賠償責任降級」**（跨 2 組真實合約：採購合約＋保密智財協議）——值得寫成正式規則。
   - **「違約金絕對金額（中文數字，如貳佰萬元→壹佰萬元）」是獨立於重複次數之外的真實能力缺口**：現有 `rule_penalty_weakened()` 只認百分比費率，完全不認中文數字寫的絕對金額，雖只驗證於 1 組合約，仍值得補上。
   - 「續約/加價/西元定義」等其餘候選都只來自同一組合約反覆測試，尚不夠格升級。
   - 詳細分析方法見 memory（`project_contract_diff.md`）。
2. **語意殘餘配對**（對齊層第 3 層）：**先做根因分析再決定**——用現有 gold set 查 Overall Detection 67%（12/18，非舊版誤植的 12/22 分母）的缺口是「配對錯」還是「配對對但規則沒判出」。**2026-07-16 找到一個真實的「配對錯」案例（P2／中低嚴重度，未進 gold set，不影響已驗證的 100% high-risk recall 宣稱）**：真實合約（pic_contract）裡舊版兩條（8.3 資安防護＋8.4 三年保密）被合併成新版一條（8.3，只剩一年），`alignment.py` 三層比對是 1:1 配對設計，配不出「兩條併一條」的關係，導致規則引擎比對到的 old_text 其實是資安防護那條、沒有「3年」文字，規則因此沒觸發（但 Verification Agent 有補上、標成 high、正常出現在最終報告，不是整條漏網）。目前只觀察到 1 次，尚不確定復發頻率，暫列已知限制，不急著修。若要修，建議先試演算法擴充（比照 `diff_engine.py` 既有的「條款編號重排補救機制」精神，加一個「刪除舊條款＋新增/修改新條款」的組合相似度偵測），不建議一開始就上新 LLM agent。

---

## 不做（賽前）

真實 PostgreSQL 資料庫、異質模型 MAS、Teams/SharePoint 整合、大規模資料持久化、雙欄排版 PDF 解析（Docling，除非遇到真實案例）。理由與技術備忘見 `archive/next_step_plan.md`。
